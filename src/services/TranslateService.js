import axios from 'axios';
import {IP} from "../constants/properties"
import {PORT} from "../constants/properties"
import {PROTOCOL} from "../constants/properties"

export default class TranslateService {

    // translate (source, target, text) {
    //         const params = {
    //             'source': source,
    //             'target': target
    //         };
    //         const url = `${PROTOCOL}://${IP}:${PORT}/api/translate`;
    //         return axios.post(
    //             url,
    //             {params})
    //             .then(response => {
    //                 response.data
    //             });
    // }
}